#include <bits/stdc++.h>

using namespace std;

typedef long long ll; 
typedef vector<int> vi; //vetor de inteiros
typedef pair<int,int> pi; 	//dois inteiros

#define REP(i,a,b) for (int = a; i <= b; i++) 

#define PB pushback


int main(){
	
	ll x = 123123123; //usado para diminuir o que se tem que digitar
	
	cout << x;
	
	//macros diminuem funçoes
	
	REP(i,a,b)
	
	
	v.PB(make_pair(y,x))/
	int d = v[i].first+v[i].second
	
	
	return 0;
}

