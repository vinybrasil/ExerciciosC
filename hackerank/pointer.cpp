#include <bits/stdc++.h>

using namespace std;

void f(int i){
	
	//int v1[i]; //erro
	vector<int>v2(i); //sem erro
	int v[4] = {1,2}; //4 pode ser maior ou igual o numero de elementos, nao menor
	sizeof("Bohr") == 5;
	char* p == "Plato"; 
	
	
	
}

int main(){
	char c = 'a';
	char* p = &c; //p pega o adress do c
	char c2 = *p;
	
	cout << c << endl;
	cout << p << endl;
	cout << c2 << endl;
	
	int* pi;
	char** pcc; //pointer para um pointer
	int (*fp)(char*); //pointer para um função que toma um char* como argumento (retorna uma int)
	
	char* a[32]; //array de 32 pointers 
				 //o tamanho da array tem que ser constante
				 
			
	
}
